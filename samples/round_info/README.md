API sample that retrieves S'Up round info.

Get your team ID and token from the URL returned by DMing the bot `set api on`.

```
export SUP_TEAM_ID=...
export SUP_API_TOKEN=...

bundle exe ruby round_info.rb
```